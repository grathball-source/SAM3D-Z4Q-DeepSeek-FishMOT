# E1C-A execution scope

This is a retrospective execution record, **not** a claim of a newly blind preregistration. The fixed review base was `12c6dcf60df86deedc71c719b867d56e8ab53838`; only a new branch and `experiments/vl_assoc_e1c/` were written. Existing E1 sealed outputs, its 24 scored events, raw requests, original inputs and GT were read-only.

Hypothesis: permitting one missing historical public identity to compete with a short current native tracklet may expose genuine recovery opportunities that the old two-owner/current-swap protocol excluded. The model, if ever authorized later, must compare complete association hypotheses with cited applicable evidence and make a nonzero selection change. This A stage makes **zero** model calls.

Fixed causal controls: 6-second event window, 15-frame wait, the old B0 stream, same split and original observation/depth streams. Prediction-only candidate replay was sealed in `EXPOSURE_MANIFEST.json` before `score_exposed.py` read exposed development/validation GT. The scorer retains missing/fragmented anchors and ambiguity as unscorable. No unexposed test GT was opened.

The planned A gate required engineering tests and at least two distinct, legal, GT-supported recovery candidates that remained beneficial in isolated follow-up. The actual probe found zero confirmed correct executable recovery candidates; seven probe rows also exceeded the requested two-current-tracklet cap. A therefore stops before any pilot freeze, model API or oracle trajectory continuation. Do not reinterpret the subsequent diagnostic code as a passed protocol.
