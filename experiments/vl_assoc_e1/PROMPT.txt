You compare COMPLETE identity association hypotheses for one causal fish-tracking event. The historical labels A/B are unverified hypotheses; current X/Y/Z are anonymous observations. Fixed observations U* keep their listed identity under every hypothesis. Do not treat native continuity, a clean mask, illumination, head direction, or a single depth value as proof of identity. Use only packet evidence available at or before query_time. Some measurements share a predicted mask and are correlated.

When an image is present, each row has an unaltered RGB context crop on the left and a closer whole-fish crop on the right. Rows follow packet.history in listed node and sample order, then packet.current in listed node and sample order. Static uses only each node's last sample; temporal uses every listed sample. The text packet supplies each row's anonymous label and true source_time. White gutters are layout, not evidence. An absent row means unavailable pixels, not a negative identity cue.

Return one JSON object with exactly schema, packet_id, snapshot_version, comparisons, applicability. Include every unordered pair of listed candidate IDs exactly once in lexical left/right order. For each pair return exactly left, right, relation, evidence_ids, reason_code, short_explanation. relation is LEFT_BETTER, RIGHT_BETTER, INDISTINGUISHABLE, or UNOBSERVABLE. A directional preference must cite at least one existing packet evidence ID; citations do not make the evidence automatically reliable. reason_code is DEPTH, APPEARANCE, MOTION, MULTI_CUE, QUALITY_LIMIT, or INSUFFICIENT. short_explanation is at most 80 characters. applicability has exactly appearance, motion, depth, mixed_mask, each APPLICABLE, UNRELIABLE, or UNKNOWN.

Compare whole mappings, including any displaced owner. Do not output probabilities, guaranteed correctness, free-form identity edits, unlisted candidates, or future observations. If evidence cannot distinguish a pair, use INDISTINGUISHABLE or UNOBSERVABLE.
