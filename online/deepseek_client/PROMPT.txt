You are assessing two focal fish identities using a causal observation packet. Select exactly one option from the supplied exhaustive catalog. The catalog includes all injective complete matches to observed nonempty candidates, possible two-fish groups within one observed mask, and unresolved/WAIT. Do not construct a new option or assume that two separately visible masks must exist. A possible group is only a hypothesis, not a fabricated mask or measured hidden location.

All coordinates and distances are measurements with limitations. Histories may be old or geometrically mixed; native lineage and frozen tracker ownership are unverified proposals, not identity truth. Short fitted motion extrapolated over a long gap may be unreliable; an offscreen prediction is not evidence that a fish vanished. Missing/invalid depth is not a zero distance. Pixel sizes are not metric body length. Appearance distances can change with illumination, pose and mask contamination. Prefer unresolved when different plausible assignments remain insufficiently distinguished. Do not simply choose the smallest sum of incomparable distances or follow baseline ownership automatically.

Read the recent observations, reference ages/spans, all candidate qualities and all pairwise evidence. If image panels are attached, they show the real RGB reference/current crops with predicted-mask outlines and opaque aliases; use them with the same numerical evidence. Mask outlines can be wrong or merged. Some image content is repeated context. If no image is attached, use only the numeric packet and do not claim to have seen a picture. There are no ground-truth identity labels or future frames in this input.

Return one JSON object and no other text, with exactly these keys:
{"option_id":"one ID from options","evidence_ids":["one or more IDs from evidence"],"reason_codes":["one or more allowed codes"]}
Allowed reason_codes: appearance, depth, motion, lineage, ambiguity, poor_measurement, missing_evidence.
Reference only supplied evidence IDs, never array paths, new IDs, or option IDs as evidence. Citation existence will be checked separately from assignment correctness. Do not output confidence, coordinates, names, explanations, or additional fields.
