# M2-T: geometry-sequence-first DeepSeek association probe

Read [PROTOCOL.md](PROTOCOL.md), [CONFIG.json](CONFIG.json), and [FINAL_REVIEW.md](FINAL_REVIEW.md) before reuse. This is a new, isolated, user-authorized experiment at review base `a27720ff313a27037eb5cbdd9d629506911af7d2`. It does not modify M1, AO1, AO0, the tracker, or their archived outputs.

The source host is `ps-ESC8000-G4` as user `xiongxiong`. The restricted run is `/home/xiongxiong/m2t_motion_first_20260924/full_run`; the true B01 input pilot is `/home/xiongxiong/m2t_motion_first_20260924/pilot_b01`. They require the AO1 `range_corrected_run` source/blind media and verified development/validation observation and assignment streams. `build.py` produces public source/request/preflight manifests plus sender-only G/AP images, logical payloads and a private native-token ledger. `runner.py` is deliberately hardwired to `/work`; mount only `full_run/sender` there. The official key is sent through stdin and is never an argument, file in the run or Git artifact. The sender does not mount the score key or repository.

The post-seal independent sequence is `score.py`, then `audit_citations.py`, then `artifact.py`. Scoring cannot open the already exposed AO1 key until all 25 response hashes and ledger records match the response seal. Pixel audit checks decisive cited G images against the actual sent bytes and source geometry, but does not make model semantic claims true. Public files are copied into this directory; `ARTIFACT_MANIFEST.json` inventories every restricted file by real path, size and hash. Do not upload `sender/media`, `sender/private`, the private native-token ledger, raw RGB/masks/GT, keys, or provider file IDs to Git.

This exposed five-case offline probe does not authorize further paid retries, a new tracker score or E2. Repeat answers are diagnostics, not votes. M1's earlier instability and four harmful negative cases remain available unchanged as historical evidence.
