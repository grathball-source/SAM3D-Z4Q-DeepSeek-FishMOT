# M3-L frozen local-correspondence protocol

Review base `8ec0d3b8bd2e7c75dd2d6b3d15b1b0bb6341e16b`. This is a new, explicitly authorized diagnostic probe, not an M2-T rerun or a tracking transaction. The fixed sample is B01 G006–G018, local frames `1300,1308,1315,1321,1323,1331,1338,1346,1353,1361,1369,1376,1384`, twelve adjacent sampled-time pairs. They are not consecutive acquisition frames and are one physical interaction. Old M2-T and M2T-F files are read-only.

Each request contains exactly two byte-identical copies of the old grayscale G geometry images, complete predicted ROI observation lists, centers, boxes and true relative time. No full-sequence role, persistent native/public ID, GT, old answer, future target image, RGB, or numerical baseline is in the message. The model must jointly choose a target observation or `UNRESOLVED` for every source observation, cite real source and target tokens for `MATCH`, and avoid double occupation. No unobserved intermediate trajectory or probabilities are requested.

The pre-registered order is P01-first, P02-first, P01-repeat, P03-first, P02-repeat, …, P12-first, P11-repeat, P12-repeat. Repeats have identical body bytes and are not votes. DeepSeek official `deepseek-flash`, Files API `file` content blocks, thinking enabled/high, JSON object, `max_tokens=65536`, concurrency one, no inference retry. One synthetic same-route smoke precedes formal calls. At most 25 inference HTTP requests and USD 3 peak-price upper accounting. The sender receives the key only through stdin inside a bubble containing its `/work` directory and system TLS/network files, with no repository, old response, score key, GT, or current chat mount.

All 24 body SHA-256 records are sealed before first formal inference. After all 24 responses and call ledger entries are sealed, a separate process validates body/response/seal hashes and then opens already exposed validation offline matches and original predicted masks. The scorer does not edit model choices. JSON/schema failure, illegal citation, occupied target conflict, valid abstention, and posthoc correctness remain distinct.

A source is scoreable only if it has one unambiguous GT and exactly one unambiguous target observation of the same GT with no other ambiguous target that might contain it. No native-ID equality is a truth label. N-C uses normalized center Euclidean cost, N-I uses one minus original-mask IoU, both rectangular Hungarian assignments with exact-primary ties broken by center then frame-local lexical token order. All objects and both baselines are reported. Path composition starts from original G006 A/B role tokens after sealing, follows only model `MATCH` edges, and stops at abstention, conflict, invalid reference or missing edge; exposed GT annotates the first certified wrong step but never repairs a path.

Pre-registered gates: 24 traceable formal outcomes and at least 23 parseable; ≥8 scoreable hops and ≥16 scoreable source observations; first and repeat precision ≥95%, coverage ≥80%, same-source decision/target agreement ≥90%, and no certified wrong step in either key starting chain. Increment additionally requires at least two distinct hops where both fixed baselines are wrong but both model attempts are right, without equal/larger added harms. These are developmental gates for one interaction, not method or IDF1/HOTA evidence. No prompt search, expansion or E2 is authorized on failure.

Source: [DeepSeek Files API](https://api-docs.deepseek.com/guides/files_api/) and [official pricing](https://api-docs.deepseek.com/quick_start/pricing/), checked 2026-09-26. Peak listed rates: input cache miss USD 0.30/M, output USD 1.20/M; each 65,536-token output cap reserves USD 0.0786432 before input allowance. Frozen USD 0.11 per attempt ×25 = USD 2.75, below USD 3; actual peak-rate accounting uses returned token usage. Provider alias is not an immutable model snapshot; returned model/IDs are recorded.
