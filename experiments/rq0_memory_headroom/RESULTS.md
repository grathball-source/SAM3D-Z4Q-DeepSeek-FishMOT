# RQ0 result — STOP_NO_VISIBLE_QUALITY_HEADROOM on this frozen operation

Fixed base `6320f29bdaf8d33ed64dd7e4888089a0f19e3319`, independent branch `codex/rq0-association-memory-go-no-go`, actual lab host `xiongxiong@10.2.212.110`. This is **not E1C rerun or E2**. `allow_api=false`; new model/API calls, paid tokens and GPU use were all zero. The new commit is recorded in the final task handoff.

| Axis | Verdict | Evidence and boundary |
| --- | --- | --- |
| Engineering | **PASS after one explicit scope correction** | A real 8400-frame ALLOW replay exactly matched archived B0 output and full engine state; one no-GT VETO changed bank/view_bank/recent_core write at `986:n:0`, left output/lifecycle/non-target state intact, and changed the later real `history:core` read at frame 1274. `CANARY_TESTS.json`, `ATOMIC_TESTS.json`, `REFERENCE_WRITE_READ_AUDIT.jsonl`. The first Q-rule branch violated the frozen-B0-write intersection for 27 vetoes; it remains sealed and was corrected without threshold change. `VETO_SCOPE_AUDIT.json`, `SCOPEFIX_OUTPUT_PARITY.json`. |
| Real input | **PASS for sampled pixels; limited coverage** | Original RGB and predicted RLE were opened, hash/area-bound, and 9 private sheets/52 sampled writes actually inspected. Two visible defects, seven UNKNOWN, 43 visually usable; 31,443 other B0 writes unreviewed. No temporal identity or physical-depth QA was claimed. `PRIVATE_VISUAL_QA.md`, `QUALITY_LABELS.jsonl`, `UNKNOWN_COVERAGE.json`. |
| Action reachability | **PASS, no output benefit** | B0 allowed 25,009 development + 6,486 validation reference writes. The visible oracle vetoed the two labeled validation writes; downstream reader changed on 10 frames and association trace on two frames, but **zero public-ID output frames changed**. The two vetoes share one continuous branch, so the ten later reader frames cannot be attributed twice. `OUTPUT_SEAL_*.json`, `REFERENCE_WRITE_READ_AUDIT.jsonl`. |
| Model evidence | **INCONCLUSIVE_NO_MODEL_CALL** | RQ0 has no VLM quality response. The two labels are human-visible quality judgments, not model performance. Prior E1 harms remain retained, not reinterpreted as a new VLM failure. |
| Full follow-up | **FAIL promotion / STOP_NO_VISIBLE_QUALITY_HEADROOM** | Oracle = B0 on every frame and every official metric in both exposed splits. All 12 split×GT individual follow-up records show zero changed oracle frames; `FOLLOWUP_HORIZONS.jsonl` expands the two validation veto origins to 30/90/150 and remaining horizons, again zero for every individual. No demonstrated pair of independent sustainable recoveries. |

## Continuous, independently reset branch results

All metrics below are percentages except counts; each row is a full continuous split scored by the original TrackEval implementation with unchanged mask set/order. The 8400-frame development and 2888-frame validation branches each start from an empty native state, so no pooled 11,288-frame score is claimed. The Q-rule shown is the separately sealed **scope-corrected** result; original invalid Q-rule outputs are preserved. The old and corrected Q-rule outputs are in fact identical on all 11,288 frames (`SCOPEFIX_OUTPUT_PARITY.json`), and the corrected official scores were independently recomputed after its new prediction seal.

| Split | Arm | IDF1 | HOTA | AssA | IDSW | FP | FN |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| development | B0 | 99.333472 | 77.829488 | 77.907638 | 6 | 194 | 323 |
| development | Q-rule scopefix | 92.002662 | 73.933507 | 70.300280 | 6 | 194 | 323 |
| development | visible oracle / quota | 99.333472 | 77.829488 | 77.907638 | 6 | 194 | 323 |
| validation | B0 | 80.697587 | 69.243869 | 60.074321 | 9 | 298 | 423 |
| validation | Q-rule scopefix | 80.228280 | 69.101687 | 59.707327 | 9 | 298 | 423 |
| validation | visible oracle / quota | 80.697587 | 69.243869 | 60.074321 | 9 | 298 | 423 |

Source: `METRICS.json`, `METRICS_QRULE_SCOPEFIX_*.json`, immutable `OUTPUT_SEAL_*.json`. Corrected Q-rule vetoes 20,532 development and 5,237 validation B0-allowed writes. This rule is visibly over-aggressive on elongated single-fish masks and harms association; it is not a successful numeric baseline. Its presence feature was not shown numerically in the manual crop sheets, so the human-label-versus-rule **input parity is incomplete**; no claim that a VLM beats this rule is made. Quota vetoed two different B0-allowed validation writes, also without output effect, and is an offline matched-count control, not deployable causal policy.

## Full-sequence error census and GT-definition boundary

`FULL_SEQUENCE_ERROR_CENSUS.jsonl` covers the complete exposed 8400+2888 frame timelines, not E1/E1C registered queries. It retains **127 non-correct intervals** (development 4 certified association-error + 29 no-unique-observation; validation 15 certified association-error + 58 no-unique-observation + 21 UNKNOWN-global-ID intervals). Of these 127, 87 have no old E1C query in the interval and 40 overlap a registered query. `FULL_SEQUENCE_ERROR_LINEAGE.jsonl` binds every interval start to the actual-host predicted RLE source file/hash and observation key when one exists; 87 no-observation intervals correctly have no native mask rather than a fabricated image. `RELATION_CENSUS_*_v2.json` records native/public changes, fixed retrospective GT↔public mapping, and its uncertainty; `FOLLOWUP_AUDIT.jsonl` preserves all GT individuals and right-censoring. The validation global mapping has two individuals without ≥0.8 dominant public ID, leaving 5,690 GT-frame relationships UNKNOWN, not silently counted as correct or harmed. The interval grouping is conservative but not a certified physical-interaction count; the long UNKNOWN interval can collapse validation clusters, so **neither the 127 intervals nor repeated queries count as independent opportunities**.

The originally generated `METRICS_{split}.json` first-anchor relation counts were methodologically unreliable on validation because early public identity was unstable; those files remain visible as the failed first pass. `relation_census_v2.py` corrects only that auxiliary relation diagnostic using a clearly retrospective full-exposed-sequence assignment with uncertainty. It does not alter sealed predictions or the official TrackEval scores and is never a gate input. `SCORER_CROSSWALK.json` exhaustively found zero binding differences or missing/extra keys between old `native_to_gt` and `objects.gt_id/ambiguity` over 50,271 development and 17,197 validation object rows; 194/298 rows are legitimately withheld as ambiguous/unmatched. Both use the stated frame-local max-cardinality-then-IoU Hungarian threshold 0.5 protocol. This crosswalk prevents the local frame-219 E1C interpretation from being promoted into an unverified whole-sequence ID repair.

## Failure cases, costs, unknowns

The four older model-harm packet IDs remain in `PRIOR_FAILURE_BINDINGS.md` and the old sealed records. RQ0's two visibly bad reference writes `validation:2578:n:1` and `validation:2747:n:7` changed real references but not final IDs; neither may be claimed as a sustained rescue. The simple rule's large veto budget and reduced IDF1 are a new harmful control result, not a VLM failure. The synthetic shared-edge E1C contract flaw is reproduced separately in `PRIOR_CONTRACT_REGRESSION.json`, without changing the old decoder.

All new compute was CPU-only on the lab server. The observed four-arm replay took roughly 1–2 minutes for development and under 30 seconds for validation; these are operator wall-time observations, not a formal latency benchmark. Additional image rendering, exposed-GT rasterization and independent scoring were offline jobs. New API spend was **$0**; no VLM token count or VLM inference latency exists. No private RGB sheet or GT raster is in Git. Main unknowns are the 31,443 unreviewed B0 writes, single-frame visual-label scope, two validation GTs without stable retrospective public mapping, and whether a different dataset/operation might have visible quality headroom.

The attachment's advancement condition fails at the first scientific effect gate: the visible oracle made no output or AssA gain, let alone two distinct sustained interactions or an advantage beyond a fair simple rule. This stops **this quality-veto operation on this exposed corpus**; it is not a theorem that every VLM quality role is impossible. `READY_FOR_SMALL_VLM_QUALITY_PILOT` is **not** delivered. No E1C-B, SAM3-memory alteration, model training, or E2 was started.
