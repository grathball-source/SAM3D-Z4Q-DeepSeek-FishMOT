# Actual private-pixel inspection, no Git media

The reviewer opened all **9 newly generated sheets / 52 selected current-frame RGB+predicted-mask crops** from `/home/xiongxiong/rq0_private_visual_20260924` (development 6 sheets/34 writes, validation 3 sheets/18 writes). `render_quality.py` verified original RGB SHA-256 against the server dataset manifest and decoded original predicted RLE masks with matching areas. Each sheet displayed the whole resized frame, source box, and a crop with the predicted mask in red. The sheets and underlying pixels remain private on the execution host; the local review copies live outside this Git repository at `E:/CAU/D-MOT/logs/rq0_private_visual_20260924`.

`QUALITY_LABELS.jsonl` binds each inspected observation key to the private sheet SHA and a no-GT reason. 43 were judged visibly usable as single-fish current-frame references, 7 stayed UNKNOWN for crossing/glare/boundary uncertainty, and **2 were visibly unusable**: validation `2578:n:1` (separated red components around the lamp/fish) and `2747:n:7` (main red fish plus a separate small fragment). These are mask-reference quality judgments, not proof of public-ID correctness. The 52 reviewed writes represent only 0.165% of 31,495 B0-admitted writes. Unreviewed writes are UNKNOWN and default ALLOW, not implicitly good.

The new sheets did **not** include earlier frames or depth-accuracy ground truth. “USABLE” here is therefore limited to one current RGB frame/mask; image identity continuity, physical depth correctness, and all 31,443 unreviewed writes remain unverified. The prior E1 four harmful temporal sheets/45 rows were verified in E1C-A (`experiments/vl_assoc_e1c/PRIVATE_VISUAL_QA.md`) but not counted as newly reviewed RQ0 reference writes. No private pixel, raw RLE, GT raster, or old private wire is committed here.
