# Execution record (2026-09-26, Asia/Shanghai)

The local checkout and fetched `origin/main` both resolved to the required review base `869d9812a6549307e8e4502d1ee17d6881494626`; the checkout was clean. The authorized source host was `ps-ESC8000-G4`, user `xiongxiong`, with 67 GiB then free on `/home` and no space on `/home/data2`. No GPU job was started. Input paths, existing run status and host resources were checked before reading source streams. All new files were written under `/home/xiongxiong/ehr1r_causal_history_20260926/`.

1. The actual old EHR-1 `run_v6/public/EPISODE_FACTS.json` failed a new source-backed check on B01/B02/B04-A before new request preparation; B03/B05 risk records were also retained. `run_v1` through `run_v4` were preparation iterations only, with zero model calls; their public config/output hashes are in `PREPARATION_INVENTORY.json`.
2. `run_v5` froze the repaired A/B/X/Y source fragments, all anonymous event observations, 55 images, and 25 logical requests. The raw HDF5 audit passed 727 new depth frames and 4332 profiles. The v5 preflight returned PASS and reserved USD 2.782218, but did **not** test complete quality on anonymous H-D depth.
3. The sender consumed a mode-600 FIFO key, ran in a `bwrap` tree containing only plan, media, gate and sender code, and used the official `deepseek-flash` Files API. Smoke `S001` started at 19:16:53 and returned at 19:16:54. All 25 file-ID-bearing request bodies were sealed before the first formal response. B01's five calls ran sequentially from 19:17:44 to 19:23:41. B02-E and B02-H-2D returned by 19:28:06; B02-H-D had started at 19:28:06.
4. A postfreeze source-contract review found that every H-D anonymous event row carried median and valid fraction but omitted `sensor_available`, `synchronized`, core n/MAD, and other required quality. At roughly 19:29 the executor verified the task-specific supervisor PID `2293197` and child `2293500`, sent TERM only to that supervisor, and verified both exited. B02-H-D has no saved raw or public response, so its HTTP outcome is **unknown** and it is never retried. `formal.exit` was not produced because the supervisor itself was terminated. The original ledger is unchanged.
5. At 19:29:58 local time, `PARTIAL_RESPONSES_SEALED.json` sealed 7 returned formal responses, 1 unknown HTTP outcome and 17 unsent formal requests. There is no full response seal or trial score. Known returned-plus-smoke peak-rate charge is USD 0.2983608; including the unknown request's USD 0.1280376 reservation gives USD 0.4263984 upper accounting. Raw choices and citation legality were read only after this partial seal.
6. Only after the partial seal, the exposed relation timeline was used to check the **new** anchor/q physical mapping. All five new endpoint mappings were uniquely identifiable in that retrospective relation file and posthoc mapping labels matched the old keys; B01's source linkage to its old contact-frame anchors remains unresolved. The first partial-binding script had compared the X/Y subset to complete X/Y/U candidate maps and incorrectly marked all unscorable; its output is preserved as `CASE_SCORE_BINDING_INITIAL_INVALID.json`. The corrected binding is `CASE_SCORE_BINDING.json`. Neither is a score of this invalid batch.
7. The root cause was fixed in current code: every anonymous depth fact now exposes source availability, synchronization, core/whole counts and MAD, overlap and risk; a missing column fails the hard preflight. Time discontinuity and available identity-version changes are also checked. An independent `run_v6` package again passed all five source checks, 727 frame depth parity, tests and 25-request budget (USD 2.968511). It was **not** sent. `corrected_unsent/OFFLINE_REQUESTS_FROZEN.json` seals its 25 logical bodies as unsent.

The compact observation/assignment streams expose no alias epoch or generation fields for these cases; the final checker will split on such fields when present, and the report does not claim they were observed. Five fixed pair-contact proxies were present, but the full online prediction-driven hard-event gate was not logged; all five remain labelled offline diagnostic controls in `TRIGGER_SCOPE_AUDIT.json`.
