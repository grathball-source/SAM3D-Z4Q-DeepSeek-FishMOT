# M1 verification

On the actual laboratory host, `/home/data2/xiongxiong/dmot-annotation/env/bin/python` ran `test_m1.py` with `M1_RUN=/home/xiongxiong/m1_real_vlm_pilot_20260924/run_v4` and `M1_AO1=/home/xiongxiong/ao1_input_fidelity_20260924/range_corrected_run/public` after the call seal. **10 tests passed, zero failures, in 0.152 s.** They cover real P0 tile/legend binding, P1-only crop restriction, P2 causal images, repeat byte-identical image/text input, reversible candidate permutation, image-off exclusion, message whitelist, counterfactual score-key isolation, choice/grounding separation, budget/call cap, and unchanged AO1 source/key hashes.

Additional live checks: the sender's mount namespace had no repository or `SCORE_KEY` access; `REQUESTS_SEALED.json` was written before first formal request; `DECISIONS_SEALED.json` was written before the independent scorer read the exposed key; `CALL_LEDGER.jsonl` has exactly 32 START/END pairs (two smoke including one pre-HTTP DNS failure, 30 formal) and no repeated attempt IDs. All 30 formal responses returned `deepseek-flash`, `finish_reason=stop`, bound choices, and usable decisions. Public files were scanned for `file-api-`, bearer tokens, apparent `sk-` keys, inline `data:image`, and the AO1 private-source path; none matched. This scan is a safeguard, not a proof of absence of every possible secret.

The initial artifact inventory attempt used `hashlib.file_digest`, unavailable in the host's system Python. It was changed to `hashlib.sha256(path.read_bytes())` and rerun successfully: 779 private media files, 63 private wire/ID files and 779 official file uploads inventoried. This post-score packaging correction did not alter the sealed sender, requests, responses or scorer.
