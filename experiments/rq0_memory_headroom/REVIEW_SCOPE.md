# RQ0 review scope

- Fixed review commit: `6320f29bdaf8d33ed64dd7e4888089a0f19e3319`; new independent branch `codex/rq0-association-memory-go-no-go`. Actual execution: `xiongxiong@10.2.212.110`, independent server checkout `/home/xiongxiong/SAM3D-Z4Q-DeepSeek-FishMOT-rq0`. Development and validation are already exposed; no unexposed test split was opened.
- Research question: can a **visible quality veto of real Z4Q association-reference writes** add sustained association benefit beyond the original quality rules? It cannot decide IDs, remove masks, alter SAM3 memory, train, or call a model. `allow_api=false`, new calls and charges zero.
- Dependencies read: root `README.md`, `EXPERIMENT_INDEX.md`, `ARCHIVE_SCOPE.md`; E1C-A `RESULTS.md`, `SUMMARY.json`, `ERROR_COVERAGE.jsonl`, `CANDIDATE_REACHABILITY_*.jsonl`, `INPUT_LINEAGE_AUDIT.json`, `PRIVATE_VISUAL_QA.md`, `probe.py`, `score_exposed.py`, `contract.py`; E1 bridge and native controller inheritance chain. Old archive/source/output files were not edited.
- Prediction-only canary, write selection, labels, protocol and `SOURCE_FREEZE.json` preceded the independent exposed-GT score. Full four-arm predictions and output seals preceded the GT scorer. Private RGB sheets and generated development GT stayed outside Git. The first Q-rule output is retained but invalid as a strict-scope comparator; one **engineering-only intersection correction**, with unchanged threshold and original sealed data preserved, was run in separately named files.
- `FULL_SEQUENCE_ERROR_CENSUS.jsonl` is a post-seal **relationship diagnostic**, not TrackEval and not a causal online gate. A first-anchor mapping yielded misleading validation counts. Its original output remains; the explicit retrospective global-assignment v2 replaces only that relation analysis. Individuals without a dominant global public ID remain UNKNOWN.
- The quality review covers 52/31,495 B0-admitted reference writes, selected from prediction-only contact-risk ranking. It is not an exhaustive visual oracle or an untouched holdout. Its two negative labels are actual visual mask-quality findings, not hidden GT-identity labels.
