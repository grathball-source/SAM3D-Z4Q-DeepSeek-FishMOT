# E1C-A handoff

Status: **STOP / ENGINEERING_FAILURE**. Fixed review base `12c6dcf60df86deedc71c719b867d56e8ab53838`; new branch `codex/vl-assoc-e1c-recovery`. See `RESULTS.md` for full evidence and `SUMMARY.json` for machine-readable axes. All source changes and generated public outputs are committed/synchronized with this branch; old sealed API and earlier experiments were not modified. `allow_api=false` throughout.

Prediction-only A output was sealed in `EXPOSURE_MANIFEST.json` before exposed-GT scoring. 289 registered events, 160 scorable, four B0 errors; six events with executable restoration candidates, **zero confirmed correct**, twelve of fourteen one-owner queries unscorable. Seven frozen probe rows exceed the specified two-current-tracklet cap. Four previously harmful old E1 cases and all non-scorable records remain visible. Four old temporal sheets/45 rows were bound to real server RGB/mask/depth/appearance; 120 private saved response/request pairs were hash-verified. No new model result, new visual packet, oracle continuation, IDF1/HOTA gain, E1C-B freeze or E2.

One next step only: prospectively register a compliant two-tracklet candidate/input protocol and audit its real packets without API calls.
