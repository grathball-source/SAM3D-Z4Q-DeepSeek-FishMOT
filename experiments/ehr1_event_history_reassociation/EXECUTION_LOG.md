# EHR-1 execution record — 2026-09-26

- Fetched `origin/main` and fast-forwarded the clean local checkout from `a566dc6` to required base `0b65e95ce41ef67978fad08fdd098532112b79b4`. Read the M3-L final/corrected score, M2-T depth boundary, AO0 case manifest, AO1 source/card, closed-loop freeze and bridge. No M3-L file was edited.
- Actual host/user: `ps-ESC8000-G4` / `xiongxiong`. Initial read-only resource check: `/home` about 55 GiB free, `/home/data2` full, about 37 GiB RAM available, unrelated GPU jobs. This run used CPU and network only, no GPU reservation. Source host and media paths were verified live.
- Preparation attempts `run_v1`–`run_v5` remain at `/home/xiongxiong/ehr1_event_history_20260926/`. `run_v1` stopped before output after system Python lacked `hashlib.file_digest`; later copies exposed packet/preflight issues and were superseded before any provider call. Only `run_v6` was sent. The final source code and exact run output are preserved separately; no prior paid response was re-used.
- `run_v6` built five event packets and 25 requests from actual observation, predicted mask, raw-depth feature and frozen AO1/M2-T records. Pre-send test passed: exact H-D/repeat text and image equality, candidate-only permutation, q cutoff, image hashes, absent GT/path/provider IDs, 26 × USD 0.11 = USD 2.86 reservation under USD 3. Twenty actual raw-HDF5 frames and 120 predicted masks independently reproduced RGB dimensions, array hashes and whole/core statistics. Three standalone decision regressions passed. B01 G006/G010/G017/G034 private mask images were actually opened for answer-exposed visual QA.
- Official [Files API](https://api-docs.deepseek.com/guides/files_api/) and [model/pricing](https://api-docs.deepseek.com/quick_start/pricing/) were checked before sending. Peak cache-miss input/output prices USD 0.30/1.20 per million tokens. `deepseek-flash`, thinking enabled/high, JSON, max 65,536 output tokens, sequential requests, no inference retry.
- Initial smoke upload failed with `URLError` because the isolated process lacked the `/run/systemd/resolve` mount; no inference request started. After correcting only the sandbox DNS mount, the same synthetic Files API smoke succeeded. The upload ledger preserves one failed upload attempt, 61 successful uploads (one smoke + 60 frozen case images), and no provider file ID is public.
- All 25 actual file-ID-bearing bodies were frozen before the first formal start; [REQUESTS_SEALED.json](REQUESTS_SEALED.json) and [REQUEST_MANIFEST.json](REQUEST_MANIFEST.json) bind logical text, media, code and body hashes. The sender mount exposed only this task's sender tree and system runtime/TLS/network; no repository, old responses, score key, GT, current chat or numeric comparator was mounted. The key traveled from the local environment via SSH stdin, remote mode-600 FIFO and sender stdin; the FIFO was removed before completion. No credential was placed in a file or argument.
- B01 ran first in Screen `ehr1_b01_20260926`, exited 0, and produced five `deepseek-flash`/`stop` outcomes: E, H-2D, H-D, exact H-D repeat and H-D candidate permutation. Response latency sum 299.129 s. Before any B02 call or scoring, an independent source lineage check found that `prepare.py`'s `role_segment` retained all same-native observations in PRE_HISTORY even after contact risk, while only the velocity fit rejected contact frames. This represented unverified track continuity as A/B history. B01 A/B frozen anchors at frame 1321 were themselves contact-risk frames; the sent 30-frame A/B histories contained 21/4 contact-risk frames. B02's anchors at 7866/7890 had 116/92 unreliable intervening frames before its frozen trigger. [LINEAGE_AUDIT_V2.json](LINEAGE_AUDIT_V2.json) gives all five cases and exact counts. The old pair-contact trigger itself was observable at the frozen frame in all five, but that does not certify each individual reference lineage or the full original Z4Q online proposal gate.
- Stopped before the remaining 20 formal requests. No in-place prompt/history repair or semantic retry occurred. The five B01 public responses and all 25 request body hashes were verified; [PARTIAL_RESPONSES_SEALED.json](PARTIAL_RESPONSES_SEALED.json) was written before the independent process opened the already exposed AO1 score key and frozen M2-T B0 results. The full-25 response seal does not exist, accurately reflecting the stop. Five B01 choices were structurally parseable and all five were `DEFER`; their scientific status is `UNSCORABLE_PROTOCOL_PRE_HISTORY_LINEAGE`. The 20 absent outcomes are explicit `NOT_SENT_ENGINEERING_STOP` rows, never zero-filled or selected away.
- One smoke plus five formal inference requests used USD 0.1407354 under peak-rate upper accounting (not a provider invoice), no retry. The run has no surviving Screen/sender process or GPU reservation. Restricted pixels, raw wire, file IDs and score raster were not put in Git.
