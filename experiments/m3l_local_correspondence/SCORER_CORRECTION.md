# Post-seal scorer taxonomy correction, with both outputs preserved

All 24 model responses were sealed before either scoring run. The first scorer (`postscore_initial_v1.py`, SHA-256 `36561cf7c1b7ff89ea370e4b232f2c2e2f0e5650d814fcb6c6eeed20f36a4f71`) wrote the root-level `SUMMARY.json`, `PAIR_RESULTS.json`, `TOKEN_MATCH_RESULTS.jsonl`, `BASELINE_RESULTS.jsonl`, `CHAIN_COMPOSITION.json`, `RESPONSE_MANIFEST.json` and `FAILURE_CASES.json`; they remain unchanged. Its root `SUMMARY.json` SHA-256 is `1ec556653b30cca650ebd434b605ca8ef66f47016ef9ce8ef5239fabf0a29518`. It classified P01-REPEAT and P05-FIRST as entire-response `BAD_CITATION`/unparseable, yielding 22/24 and a technical-gate failure.

Inspection of those **already sealed** response contents showed complete, unique source rows, real source/target image IDs and tokens, and no target conflict. The defect was narrower: all six `evidence[].observation` values in each of those two responses were coordinate/bbox **objects**, whereas the frozen citation contract requires nonempty text. The corrected independent scorer (`postscore.py`, SHA-256 `4bd4127710f66fb9534bc511034ee39fa0fab6aebfafb910fc6a6de06b96d0ce`) separates parseable decision structure from citation format. It keeps those twelve raw target choices visible, marks all twelve citations invalid and all twelve edges unusable, and does **not** silently accept them, replace them with numeric matches, or change any model output. The corrected outputs are only under `final_score/`; their `SUMMARY.json` SHA-256 is `df3a0b0a1d372cb8d849689da581f59e2373c77a0657770b5170a3280660cda1`. No additional model call, prompt change, sample change or GT-based choice repair occurred.

The corrected 24/24 parseability result is not a claim that 24/24 citations are legal. The two citation-invalid attempts remain failures of the requested evidence format. Accepted-match precision and coverage are computed only from valid, conflict-free edges. Raw repeat target/abstain agreement (95.45%) and usable-edge agreement (78.79%) are reported separately. The scientific no-increment conclusion is unchanged: the fixed N-I reference is correct on all 66 scoreable sources, and there is no hop where both fixed numeric references fail while both model attempts succeed.
