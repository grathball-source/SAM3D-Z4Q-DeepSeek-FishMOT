# One next step

Obtain the user's explicit decision on authorizing the already frozen `VL_ASSOC_E1` DeepSeek paid calls, with a hard $5 cap and at most 120 formal plus two non-research technical-smoke calls. If authorization is denied or absent, keep `allow_api=false` and stop at `READY_FOR_E1_API`. If authorized, revalidate `FREEZE.json` and current server resources, execute only this E1 offline paired request set, seal all outputs, then let an independent scorer read GT; do not retune or start E2 in this step.
