# M2-T frozen motion-first association probe

Fixed review base: `a27720ff313a27037eb5cbdd9d629506911af7d2`. Five *already exposed*, post-selected AO1/M1 cases B01–B05, original query, endpoint roles and physical two-candidate association only. B01 is the single AO0 oracle-action existence case; B02–B05 are the four old harmful negatives. B04/B05 overlap in a longer interaction. This is not independent generalization or a tracker-closure evaluation.

`build.py` reads the frozen AO1 source manifest, actual same-host original RGB only to verify the frozen AP crops, predicted mask RLE, original observations and timestamps. No answer key enters building. Each case's v2 window is sampled at 32 integer-uniform real frame indices including endpoints; original endpoint samples are unioned. A single ROI is the union of participating *predicted* masks at those times, padded 20% per axis and clipped to the 640×360 mask canvas. It includes neighboring masks and does not split mixed masks. Grayscale G tiles show mask fill/outline and per-frame local `f###:o##` labels; role labels and temporal rows are only for frozen endpoints. There is no persistent native/public ID in sender text or cross-frame line. The same G payload is an exact prefix of G+AP, which only adds AO1 pixel-exact RGB endpoint crops. G-END removes all non-endpoint images and measurement rows. No post-query frame, GT, score, old answer, B0 label or original path is sent.

Five calls per case: G-SEQ, its exact repeat, G+AP, its exact repeat, and G-END. Seed `202609242` fixes role aliasing, edge order and the interleaved schedule; first answers are not replaced by repeat. The stateless official `deepseek-flash` call uses the previously working Files API image blocks, thinking enabled, high reasoning effort, JSON object, `max_tokens=65536`, and concurrency one. One synthetic color-half image smoke checks the same Files API route. No semantic retry; every attempted inference consumes one of at most 26 slots. The sender sees only `/work` with sender script, frozen plan, media and its output files. The key arrives on stdin; provider file IDs, raw wire, reasoning and pixels stay private.

`runner.py` uploads images, freezes all 25 *exact* bodies and hashes before the first formal call, then logs every call and seals all responses. Only then may independent `score.py` read the exposed AO1 score key. It first verifies the exact response hashes, request/attempt mapping and all ledger entries. Schema validity, citation source legality, claimed visual meaning and actual identity correctness are separate. Every edge is SUPPORT (+1), CONTRADICT (−1) or UNRESOLVED (0); invalid directional evidence is `INVALID_EVIDENCE` and cannot act as a zero-score rival. A candidate is decoded only if both of its edges are legally SUPPORT and the competing pair sums to at most zero. Otherwise ABSTAIN. This is the same decoder across every arm. Model-off, legal edge permutation and symmetry regressions are engineering checks, not additional model evidence.

The endpoint-only numeric N-M uses least-squares velocities from at least three distinct real times on each side, the given bidirectional constant-velocity residual divided by twice the fixed ROI diagonal, and no middle frames. It is unavailable where the frozen endpoint sampling is insufficient; no zero-velocity fill or threshold. N-M is a weak endpoint comparison, not a full same-information numeric baseline. B0 is reported separately. Neither offline model choice nor AO0 oracle result is translated into a new IDF1/HOTA score.

Technical completion requires 25 traceable formal outcomes, at least 24 schema-valid, and both B01 G-SEQ outputs parseable. The tentative exposed-case signal additionally requires B01 G-SEQ first and repeat correct, no wrong G-SEQ first/repeat change on B02–B05, and legally localized temporal evidence. A model citation is not itself semantic verification: decisive citations require later visual/geometric review with reviewer-answer-exposure disclosed. Failure stops this frozen definition; no prompt/frame/candidate retuning on these five cases.
