# M1 real DeepSeek VLM pilot

Final result: [engineering PASS, frozen model-evidence gate FAIL, scientific INCONCLUSIVE_UNSTABLE / STOP](FINAL_REVIEW.md). The [execution log](EXECUTION_LOG.md), [ten-test report](TEST_REPORT.md), [limited post-score visual QA](VISUAL_QA.md), [five-event outcomes](EVENT_RESULTS.jsonl) and [private artifact inventory](ARTIFACT_MANIFEST.json) are the current entry points. Thirty real research calls and two technical smoke attempts were counted; peak-price/uncertain-charge upper accounting was USD 1.1983182, not a provider invoice. No new tracker metrics or E2 run followed.

The [origin/main delivery record](MAIN_DELIVERY.md) lists the public commit and remote tree checks separately from deliberately restricted media.

This is the new, isolated five-case pilot from AO1 commit `bdf1ba5a1406be136fb65acd085592503e35865c`. It has explicit user authorization for at most 32 official `deepseek-flash` inference HTTP calls and USD 5 total, limited to the frozen necessary fish images. It does not alter AO1, E1, AO0, any tracker state or old scores. Restricted media and file IDs remain outside Git.

Read `PROTOCOL.md`, `INPUT_ADAPTER_AUDIT.json`, `REQUEST_MANIFEST.json`, `PREFLIGHT.json` and `REQUESTS_SEALED.json` before interpreting any model result. The authoritative private run is `/home/xiongxiong/m1_real_vlm_pilot_20260924/run_v4`; `run_v1`–`run_v3` retain pre-research engineering corrections and the original two smoke calls. Their byte-identical smoke ledger/response and successful file-upload ledger were copied into `run_v4` with hashes checked, not re-billed. `adapter.py` reads AO1's frozen source/anonymous cards and old visual layout records only. `runner.py` sees a separate filesystem bubble containing its plan and exact media but no repository or score key. `score.py` reads the exposed score key only after all response files are sealed.

Official model documentation checked on the execution date is recorded in `DOCS_SNAPSHOT.json`. Peak-price cost is a conservative upper accounting, not a provider invoice. The official Files API receives exact hashed media without a public image host; file IDs are private and expire after 30 days. If service or budget blocks complete transmission, do not drop frames, resize, retune, or count a partial run as model failure.
