# AO0 query-cutoff input review

Reviewer: Codex visual inspection only, **not** an independent human relabeling or a model trial. `render_packet.py` read the actual lab-host V1 original RGB and original predicted RLE for nine frames, verified each RGB SHA-256 against the dataset manifest and each selected mask area against the observation stream. Two private PNGs were opened and visually inspected: `ao0_whole_fish.png` (ten anonymized whole-fish views from frames 1300, 1321, 1484, 1490, 1498) and `ao0_interaction_local.png` (one fixed-coordinate local crop at 1322, 1343, 1359, 1419). The latest input is frame 1498 / global 10798; no future frame, GT raster, public/native number, or persistent color label is shown in the sheets. The same red tint marks a predicted mask, not an identity.

Actual visual observations: the contact views show overlapping or crossing elongated fish, particularly at 1359. The two reference fish and two current fish are all dark/low-contrast under variable illumination; some selected reference masks are close to other fish. The whole-fish and fixed-coordinate contact views do **not** reveal a confidently localizable, stable individual marking that independently proves `R-A→C-Y` and `R-B→C-X` rather than the keep mapping. Sparse snapshots do not establish an uninterrupted physical trajectory across the contact. This is `UNKNOWN_INPUT_IDENTIFIABILITY`, not proof that images are intrinsically uninformative. No second blinded reviewer was available, and the analyst had already seen the exposed-GT event definition.

Prediction-only missing/mixed flags for the displayed native 1/4 masks: all requested masks were present and decoded. At 1300, reference A had neighbor `[5]` while B had none; at 1321 both had `[5]`. The interaction snapshots at 1322/1343/1359/1419 flagged reciprocal 1↔4 contact (at 1359, native 1 also had neighbor 3), so these are contact/mixing **risks**, not clean identity references. At current frames 1484/1490/1498, both fragments had empty neighbor lists. Exact per-frame timestamps, source RGB paths/hashes, mask hashes and crop coordinates are in `VISUAL_INPUT_INDEX.json`; no GT match or future frame is an input field.

The existing E1C D/A/M comparator was applied to exactly the causal sample frames listed in `NUMERIC_COMPARISON.json`, with the complete KEEP/SWAP mappings and original comparator formula. `B:X` and `B:Y` had finite D/A/M values, but both `A` edges were inapplicable because the sampled history A observations had predicted contact/neighbor flags. The comparator therefore returned `NO_EVIDENCE_KEEP`—a fallback, **not** active numerical support for KEEP or evidence that a VLM would win. No weight, null cost, threshold, or sample was tuned after this result.

Private media remain outside Git at `/home/xiongxiong/ao0_private_visual_20260924/` and `E:/CAU/D-MOT/logs/ao0_private_visual_20260924/`. Public `VISUAL_INPUT_INDEX.json` records frame/time, RGB and predicted-mask hashes/areas, fixed local crop bounds, image size/hash and source paths, but not the pixel bytes. This visual review covers only nine original frames and two sheets, not every frame of the 1322–1419 contact or the 1498–2888 follow-up. Depth values were read for numeric comparison; physical depth accuracy was not visually or metrically validated.
