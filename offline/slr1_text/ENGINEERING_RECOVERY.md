# API工程预算修正（不读取效果真值）

2026-09-20 13:34检查首轮4份已返回响应：8192 completion tokens被思考耗尽，finish_reason=length，final content为空，无可评分提案。第五请求可能已发出但停止前未返回，计费状态未知，不能宣称仅4次调用计费。核实Windows进程39416及父100104、命令均属于本任务后仅停止该API worker；原始响应、模型封存、输入和日志保留在父目录，未进行效果评价。

后续独立engineering_r2修复两个工程项：输出上限8192→16384，thinking=enabled/high、模型、提示、36包设计、事件与效果门槛不变；前两次请求作为技术验收，任何输出再次截断则自动停，不继续消耗后续队列。最终必须披露实际全部调用与工程尝试，不能称总共只调用72次。

补充输入检查发现PCA主轴符号未与旧描述符统一，37个重叠当前mask全部一致，35个描述符逐值相同，2个边界像素造成最大分量差0.0013531007324035227。这不改变测量定义，但破坏旧尺度严格复现；r2固定主轴正向后重新构建，同一份新包同时交给B1和LLM，要求全部旧重叠描述符精确回归后才调用。旧包及预测不改，未查看GT效果，不按答案调参。
