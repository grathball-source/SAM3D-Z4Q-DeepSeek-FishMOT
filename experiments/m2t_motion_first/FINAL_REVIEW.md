# M2-T final review — frozen version stopped

[SCORE_PROVENANCE.json](SCORE_PROVENANCE.json) fixes the exact request/response seal and exposed answer-key hashes.
[DELIVERY_VERIFICATION.md](DELIVERY_VERIFICATION.md) records the actual non-force GitHub main push and remote-tree check.

Research result: **FAIL / STOP_FROZEN_M2T** on five already exposed cases. Engineering: **PASS**. Source-input fidelity: **PASS**, but whether the allowed sparse geometry is identity-discriminative is **INCONCLUSIVE**. Both complete candidates were reachable and unchanged. VLM evidence was source-citable and causally entered the offline decoder, yet the required B01 positive recovery did not occur and two old negative cases became wrong on exact G-SEQ repeat. This is neither METHOD PASS nor E2 permission.

Fixed review/initial main base `a27720ff313a27037eb5cbdd9d629506911af7d2`; work is isolated under `experiments/m2t_motion_first/`. Read [PROTOCOL.md](PROTOCOL.md), [RESULTS.md](RESULTS.md), [EXECUTION_LOG.md](EXECUTION_LOG.md), [25 attempt rows](ATTEMPT_RESULTS.jsonl), [five paired cases](EVENT_RESULTS.jsonl), [complete call ledger](CALL_LEDGER.jsonl), [request seal](REQUESTS_SEALED.json), [response seal](RESPONSES_SEALED.json) and [restricted artifact inventory](ARTIFACT_MANIFEST.json) together. The old M1, AO1, AO0 and four VLM-harm records are unchanged.

Implementation evidence: `build.py:62` fixes real frame sampling and endpoint union, `:71` checks original assignment RLE/time, `:95` fixes one predicted-mask ROI, `:138` renders per-frame anonymous G masks without RGB/identity lines, `:192` pixel-checks only original endpoint RGB, `:218` implements endpoint N-M, and `:259` builds all five paired arms. `runner.py:147` builds two-message, stateless Files API payloads with G-prefix equality; `:167` freezes exact bodies before formal calls; `:205` records raw/public responses, usage and cost under the 26-request/USD 3 cap. `score.py:30` validates actual cited G image/time/token, `:56` keeps schema/source/semantics distinct, `:90` applies the one fixed 2×2 decoder, and `:103` independently verifies body/response hashes and ledger mapping **before** reading the exposed key. `audit_citations.py:13` opens actual sent pixels for every decisive citation. Six source-host tests passed, including future-frame rejection, anonymous grayscale, exact repeats, model-off, legal evidence permutation, invalid citation and symmetry abstention. Tests establish wiring, not model accuracy.

Actual input coverage: five unchanged queries and original AO1 endpoint roles; 32 uniformly sampled real window frames per case plus frozen endpoints, yielding 181 G frame references, 55 AP references, 233 unique media and 872 image references across 25 calls. ROI and source details are in [SOURCE_MANIFEST.json](SOURCE_MANIFEST.json) and [INPUT_AUDIT.json](INPUT_AUDIT.json). Real RGB, prediction masks and timestamps were verified on `ps-ESC8000-G4`; a true B01 pilot preceded batch build. No frame interpolation, per-frame recentering, mixed-mask splitting, native/public cross-frame line or future frame was introduced. Depth was not a model feature; existing stream hashes remain in [DEPTH_BOUNDARY.json](DEPTH_BOUNDARY.json). The two physical one-to-one hypotheses map to original candidates by frozen aliases, with no arbitrary ID edit.

Requests were sealed before the first formal HTTP call; all 25 formal responses were sealed before the independent scorer opened the already exposed AO1 key. Actual `deepseek-flash` usage: 25 formal + one same-route synthetic smoke, no retries, 26/26 outcomes, 25/25 parseable schemas, 80/80 source-legal directional edges. Charge-rate upper estimate **USD 1.2293283** of USD 3 (not a final invoice); summed formal latency **3,475.95 s**, mean **139.04 s**, max **216.81 s**. [RESULTS.md](RESULTS.md) gives the complete paired comparison. B01 G-SEQ first/repeat both abstain; G+AP first/repeat both wrongly choose the B0 relation. B03/B04 G-SEQ first abstain but repeat wrongly alters a correct B0; G-END wrongly alters both. B02 G-SEQ is correct; B05 abstains. G-SEQ and G+AP do not establish a consistent appearance effect; G-SEQ versus G-END suggests some first-response information change but no repeat-stable positive recovery. B04/B05 overlap and are not independent replicates.

The VLM is more than an explanation-only sidecar: ten real outputs decoded to complete associations; the off/valid-permutation unit tests change the decoded association. No resulting model output was committed to the tracker, and neither this offline choice count nor the AO0 oracle stage result is a new IDF1/HOTA score. All 106 decisive citations were actual-G-byte and geometry source-valid; [VISUAL_QA.md](VISUAL_QA.md) records eight pre-score and six post-score manually opened images. Cross-time semantic continuity remained **UNKNOWN** without independent adjudication, especially in crowded/mixed frames. This limitation cannot be silently converted into either correct identity evidence or proof that the original full video lacks such information.

Known failure modes and boundaries: sparse 32-frame sampling may skip decisive motion; G retains mask shape/scale and mask errors, so it is not pure motion; AP crops can contain neighbors, so their effect is not pure texture; N-M is unavailable for three cases and wrong in the two available ones, but is endpoint-only rather than same-information; these five pre-exposed cases cannot support generalization; no independent first-look reviewer assessed semantics. The result stops this exact prompt/input/decoder definition. No further tuning, extra models, old API replay, hidden test GT, training, tracker stage/commit or E2 was performed.

Delivery boundary: all public code, configuration, tests, 25 response records, logs, manifests, paired results, failure review and reports are committed to and non-force synchronized with `origin/main`; the final task handoff records the exact verified tip SHA. Private pixel media, provider file IDs, raw wire, sender bodies and token ledger are **not** in Git; [ARTIFACT_MANIFEST.json](ARTIFACT_MANIFEST.json) lists 333 restricted files by real path/size/SHA and their reproduction dependency. No artifact is hidden merely behind an unexplained hash.

One next step only: establish a **new, non-overlapping, not-yet-scored interaction cohort** with query-cutoff identity evidence audited before deciding whether any separate model authorization is warranted. Do not retune or replay this five-case frozen batch.
