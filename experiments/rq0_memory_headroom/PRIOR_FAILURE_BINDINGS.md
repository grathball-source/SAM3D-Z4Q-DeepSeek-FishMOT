# E1C-A and old model failure bindings (read-only)

The E1C-A `RESULTS.md`/`SUMMARY.json` records 289 registered events, 256 queries, 160 scorable, 14 one-owner recovery queries, six with executable candidates, **zero confirmed correct executable recovery**, and seven >2-tracklet violations. Its 13 isolated contract checks did not exercise a real E1C packet or atomic continuation. No new E1C model call occurred. These facts are not evidence of a new VLM quality failure.

- `ERROR_COVERAGE.jsonl:202`, `validation:2-3:1`, query 219/global 9519: historical public 2 references 86/91/96 bind GT1; current native 6 binds GT1. Frozen recovery candidates are native 1→2 and native 5→2, both executable but GT-wrong. `probe.py:52-58` sorts by distance from the fixed contact center and truncates to two without logging every excluded object. The first specific blocker for native 6 remains **UNKNOWN** without replaying the full candidate pool. This event-local native 6 reference is not the same definition as an earlier native 7→public 2 semantic example.
- `ERROR_COVERAGE.jsonl:234`, `validation:1-4:5`, query 1435: event-local swap 1↔4 is GT-correct against that event's refs but `bridge.py:72-80` rejects it as `current_quality_or_contact`. No source trace here isolates neighbor contact versus quality or depth as the sole cause.
- `ERROR_COVERAGE.jsonl:245`, `validation:1-5:7`, query 2092: new event-local public 1 history binds GT6 whereas query 1435's earlier public 1 history bound GT2. Rebuilding reference semantics per event cannot certify long-term identity; this observation alone does not prove a particular engine-memory contamination path.
- `probe.py:117-127` starts its 15-frame clock once the two old owners are no longer a contacting pair, including a missing owner, queries once, then removes the episode. It does **not** wait for a newly re-established stable tracklet. `INPUT_LINEAGE_AUDIT.json` found no missing/public switch in the 48 old current segments but 17 contact-state transitions; that is not proof that a lineage defect caused the four wrong old choices. Old `score_exposed.py:106-120` only loads reference/query frames, not a full error timeline. `Bridge.next_epochs` at `bridge.py:60-62` is already repaired and is not an RQ0 defect.
- `PRIOR_CONTRACT_REGRESSION.json` reproduces a separate synthetic defect: C1={X:A,Y:K}, C2={X:A,Y:B}, citing only shared A:X is accepted as a grounded directional preference by `contract.validate_response` at `contract.py:99-119`. The old decoder was not edited; this is not a new actual model error.

All four old harmful E1 packet IDs are retained in `experiments/vl_assoc_e1c/FAILURE_CASES.md:7-10`: `P4218132a1659f1e2`, `P04c303d9612421d6`, `P175d295a01799fe5`, `P1797ad89bd359331`. They were not screened out or counted as new RQ0 positive examples. Their prior visual review is not a fresh RQ0 pixel audit.
