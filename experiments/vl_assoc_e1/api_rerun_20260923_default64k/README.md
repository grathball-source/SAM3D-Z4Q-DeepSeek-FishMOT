# E1 full 24-event rerun: new independent output

This is an explicitly authorized **exploratory rerun of all 24 frozen E1 events**, from the first request. It does not reuse old API responses or GT decisions, replace `api_run_20260923`, or reverse its early-stop conclusion. Seven events were already scored in the older run, so this rerun is not a fresh blind validation even though inference itself remains GT-blind. No E2 or full-sequence trajectory edit is authorized.

For every request, the `max_tokens` field is **omitted**. As of 2026-09-23, DeepSeek Chat Completions documents a 64K default in thinking mode; omission is not infinite output. The original prompt, frozen packet/media hashes, 24 events, seeded schedule, B0/N controls, three model arms and two diagnostic arms remain unchanged. The user authorized a new $20 hard spending ceiling and a 90% aggregate validity threshold; each individual response must still finish normally and satisfy the complete association/evidence contract. At least 108/120 valid formal responses are needed; after more than 12 invalid responses the stage stops without retries.

`COST_PLAN.json` reserves $10.8568242 at peak, uncached prices for 120 formal plus two smoke calls, using the frozen conservative input-token estimates and the provider's 64K thinking-mode default. `REQUEST_FREEZE.json` binds plan and execution code hashes. Public request metadata contains hashes, not source image base64. Wire bodies, raw responses and the transient key FIFO stay under ignored `private_api/` on the execution server. Output is sealed before any independent E1 GT scoring. `allow_api=true` applies only to this new directory's explicit authorization overlay; the original frozen `CONFIG.json` and `API_AUTHORIZATION.json` remain false.
