# M3-L: B01 adjacent local-observation correspondence

Read [FINAL_REVIEW.md](FINAL_REVIEW.md) for the final layered decision, [PROTOCOL.md](PROTOCOL.md) for the frozen new hypothesis, [final_score/SUMMARY.json](final_score/SUMMARY.json) for the authoritative gate, [final_score/PAIR_RESULTS.json](final_score/PAIR_RESULTS.json) for all 24 formal outcomes, and [final_score/TOKEN_MATCH_RESULTS.jsonl](final_score/TOKEN_MATCH_RESULTS.jsonl) for every source observation. [final_score/BASELINE_RESULTS.jsonl](final_score/BASELINE_RESULTS.jsonl) reports both fixed numeric comparators; [final_score/CHAIN_COMPOSITION.json](final_score/CHAIN_COMPOSITION.json) stops rather than bridging abstentions. The root-level score files are the **preserved superseded first scorer**, not the authoritative result; see [SCORER_CORRECTION.md](SCORER_CORRECTION.md). [CALL_LEDGER.jsonl](CALL_LEDGER.jsonl), both seals, manifests, and [ARTIFACT_MANIFEST.json](ARTIFACT_MANIFEST.json) document billing and restricted inputs/wire. This is one exposed interaction, not a full tracking or generalization trial.

`prepare.py` checks and copies only the 13 old B01 G images, creates a real two-frame slice audit, and freezes all 12 anonymous local requests without opening GT. `sender.py` is copied into the server's isolated sender tree and run in a bubble with only that tree at `/work`; it verifies the technical image route, uploads the 13 already authorized images, freezes all 24 file-ID-bearing request bodies, and makes the 24 formal calls once each. `postscore.py` is a separate process that refuses to open the already exposed validation match source until all 24 bodies, responses and ledger entries are sealed and rehashed. `test_m3l.py` contains contract regressions; `visualize.py` makes an answer-exposed, audit-only panel after scoring. `artifact.py` inventories, without publishing, the restricted pixels, original token ledger, private wire and GT-match source.

The old M2-T and M2T-F experiments and conclusions are read-only. [REVIEW_ADDENDUM.md](REVIEW_ADDENDUM.md) limits two old text-screening claims without rerunning that audit. There is no new tracker state change, training, hidden test GT, or IDF1/HOTA result.
