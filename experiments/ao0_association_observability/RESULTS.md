# AO0: one executable exposed identity-relation correction, no model evidence

Fixed review base `6d090f9b6482eecbd769177a058f506ea16a129d` on `origin/main` at start; laboratory host `xiongxiong@10.2.212.110`, isolated checkout `/home/xiongxiong/SAM3D-Z4Q-DeepSeek-FishMOT-ao0`. `allow_api=false`; model/API calls, training and GPU allocation: zero. This is AO0 only, not RQ0 rerun, E1C-B or E2. The archive's older results are read-only.

## Complete relation diagnosis and case freeze

`relation_audit.py:audit` joins sealed RQ0 B0 predictions, exposed-GT truth and existing ambiguity-filtered `objects.gt_id` matches. It emits every GT/frame's time, native/mask key, public ID, match IoU or reason no unique observation, the full GT×public co-occurrence matrix, and contiguous relation segments. The first diagnostic files under `development/` and `validation/` remain preserved; `_rel_v2/` is authoritative because it further classifies no-unique causes without changing any public relation or TrackEval score. Counts: 50,400 development and 17,322 validation GT-frame rows, with 6 and 9 temporal public-ID transitions respectively. No global majority-ID threshold discards GT2/GT6; no absolute public number is called an intrinsic GT identity. The synthetic permutation, half-sequence swap, fragmentation and UNKNOWN tests passed. The unchanged archived TrackEval B0 values are independently reproduced in `score_counterfactual.py:score`.

Validation co-occurrence: GT2 public 1/4 = 1,343/1,505 unique frames; GT6 public 4/1 = 1,335/1,507. This roughly half-and-half occupancy explains why RQ0's `>=0.8` auxiliary diagnosis withheld 5,690 relations. It does not erase the observed temporal change: GT2 1→4 and GT6 4→1 at frame 1359 after a prediction-registered 1322–1419 contact episode. They count as **one** interaction, not two. The pre-intervention frame-1300 GT anchor and query rule are frozen in `CASE_MANIFEST.json`, `CASES.jsonl`, `PROTOCOL.md`; the original matching-source hash transcription typo is transparently retained and corrected only in `CASE_MANIFEST_CORRECTION.json`. Neither case selection nor any pre-score output changed.

## Three separated causal layers

At query frame 1498 / global 10798 / time 1787709101.092, two current fragments had stable native+epoch over 1484–1498 and passed the original quality/no-contact checks. `counterfactual.py:run` generated a complete KEEP and forward public-ID 1↔4 output-only SWAP, preserving every mask, other public ID and every pre-query output. It separately replayed the same pre-query history through the original `Bridge.preview`, `stage`, `commit_once`; the swap was accepted and its own state advanced to frame 2888. `STATE_ACTION.json` records each gate and both anchors. The action is an oracle-requested diagnostic transaction, not a VLM decision. The output oracle and actual state branch agree for all 2,888 frames; each differs from B0 on the 1,391 post-query output frames. No future B0 state was injected into the edited branch.

| Full validation branch | IDF1 | HOTA | AssA | IDSW | FP | FN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| B0 / KEEP | 80.6975868362 | 69.2438689597 | 60.0743214554 | 9 | 298 | 423 |
| Output-only SWAP oracle | 94.6029722761 | 76.9416134731 | 74.1920538936 | 11 | 298 | 423 |
| Real state commit, own continuation | 94.6029722761 | 76.9416134731 | 74.1920538936 | 11 | 298 | 423 |

`METRICS.json` has the independent scorer source hashes and complete precision. Its all-object 30/90/150-frame cumulative follow-up and remaining split includes GT1–GT6, not just the two participants. At 30/90/150 frames, GT2 and GT6 each have 30/90/150 changed, uniquely matched observations with the pre-contact anchor relation restored. In the remaining 1,241 frames, GT2 has 1,216 and GT6 1,218 uniquely matched changed observations, all anchor-restored; missing/mixed frames stay unscored as identity relations, not dropped from TrackEval. GT1/3/4/5 have **zero changed public observations**. GT5 has 26 late B0 fragment frames in its own record; this branch neither fixes nor hides them. All three fixed horizons have full follow-up (no right censoring). `IDSW` rising by two is disclosed, not redefined away. These are single-case whole-split scores, not additive oracle gains or natural error-rate estimates.

## Input evidence, exclusions, tests and limits

Actual original RGB and predicted masks were source-hash/area checked for nine frames; two private sheets were opened. `OBSERVABILITY_REVIEW.md` and `VISUAL_INPUT_INDEX.json` give exact range and reasons for UNKNOWN. The visible fish are similar low-contrast elongated bodies, and sparse frames through a crossing do not independently identify the required swap. The E1C D/A/M comparator used one fixed causal packet: one historical side had no clean applicable sample, so it returned `NO_EVIDENCE_KEEP`, not an active KEEP judgment. Neither conclusion is a VLM result. No independent human blinded review was obtained.

`CASE_ELIGIBILITY.md` records why no second case was promoted; the old four E1 harmful packet IDs remain in `CASES.jsonl` as negative controls, not reinterpreted or rerun. Seven AO0 unit/atomic checks passed, including full oracle/state row equality and the independent RQ0 risk-order spacing regression; the latter documents an old selection defect without rerunning RQ0 or changing its sealed outputs. One preparatory contact probe was invalid because of a clipped scan bound and is preserved; the widened, frozen probe established the actual query. An independent scorer first stopped on a mistyped manifest match hash, then a scorer field-name error; the corrected scorer used unchanged sealed predictions and reproduced the archived B0 official metrics. The optional TrackEval BURST import emitted a missing-`tabulate` warning but CLEAR/Identity/HOTA evaluation completed; no package was installed or shared environment changed.

Conclusion: one real relationship had both output-only headroom and a working state interface, but allowable-input identifiability and independent repeatability are unproven. Engineering is **PASS** for this slice; scientific status is **EXISTENCE_ONLY / INCONCLUSIVE_INPUT / STOP_NOT_READY**, never `METHOD PASS`. See `DECISION.md` for the single next step. All public AO0 outputs and this report are to be committed to `main`; private pixels/GT raster remain outside Git and are listed in `ARTIFACT_MANIFEST.json`.
