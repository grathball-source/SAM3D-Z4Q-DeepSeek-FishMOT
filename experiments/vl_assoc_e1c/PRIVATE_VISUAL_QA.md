# Actual-host visual verification boundary

On `xiongxiong@10.2.212.110`, `visual_audit.py` opened original V1 RGB files, the original predicted RLE-mask assignments, causal depth rows, existing appearance-feature rows, the four old saved temporal sheets, the old per-row manifests and B0 owner stream. It checked original RGB hashes against `manifest.jsonl`, sheet hashes and dimensions, decoded 640×360 mask byte hashes and areas, source timestamps, and native-mask joins to depth/appearance for **all 45 crop rows across the four old harmful packets**. `PRIVATE_VISUAL_QA.json` records per-packet results. The four actual temporal sheets were also inspected visually in this task: real fish are present, with nearby fish, variable green/blue light and a strong lamp; white gutters reflect layout/crop aspect ratio. A crop's apparent continuity is not identity GT.

This is **4/24 old packets**, not a human review of all 48 old sheets and not QA of any new E1C visual input. The older automated E1 audit reported 48/48 generated sheets; that historical report does not substitute for an E1C pilot-image audit. No new E1C image packet, pixel-level evidence ID, whole-fish/interaction paired view or private wire was generated. Raw RGB, masks, exposed GT and old private wire were in fact readable on the execution host, so their absence is not used as an excuse for the A failure. Images and wire bytes remain out of Git.
