# M1 execution and isolation log — 2026-09-24

Review base: `bdf1ba5a1406be136fb65acd085592503e35865c`; independent local branch `codex/m1-real-vlm-pilot-main-sync`; actual execution host `ps-ESC8000-G4`, account `xiongxiong`, private parent `/home/xiongxiong/m1_real_vlm_pilot_20260924`. The AO1 `range_corrected_run` source/blind media were read-only. Runtime checks confirmed the expected source directories, interpreter, available disk/RAM and unrelated GPU work; M1 used no GPU. The official DeepSeek key existed only in the local environment and was streamed into the isolated sender's standard input. It was never printed, stored in the run or committed.

`run_v1` failed after preparing images due to a print-call compatibility error, before any inference; `run_v2` failed the technical-smoke reserve assertion before any HTTP. In `run_v3`, smoke `S001` stopped at DNS resolution before a service response; it remains counted and fully reserve-charged in the cap. The mount's resolver was fixed, and `S002` used the last permitted smoke slot. Its synthetic red/blue image was recognized in a real official response. `run_v3` began exact Files API uploads and was stopped at 314 files before any formal research inference, because the candidate-order seed needed to be explicitly independent of the answer. The successful upload ledger and two smoke records were byte-checked and moved into `run_v4`, not repeated or re-billed. Preliminary directories remain private and are summarized in `ARTIFACT_MANIFEST.json`.

`run_v4` regenerated all 30 answer-free packets using the fixed seed, passed the 10-test host suite, and resumed the exact image uploads to 779/779 unique SHA-256 media (2,855 image references across requests). The sender ran in a restricted filesystem mount with neither repository nor AO1 `SCORE_KEY` visible. The 30 complete request bodies and hashes were sealed at `2026-09-24T08:49:49.532927+00:00`, **before** the first research inference. The 15 first P0/P1/P2 calls were followed by 15 frozen repeat/permutation/image-off diagnostics, sequentially; none was retried. All 30 service responses were stored and the decision seal was written at `2026-09-24T09:15:28.988531+00:00`. Only then did the separate scorer read the already exposed AO1 key and write the five-case results.

The model call ledger has 32 START and 32 END records: one pre-service DNS failure conservatively reserved at USD 0.0808788, one successful image smoke at a peak-price estimate of USD 0.0002241, and 30 completed research calls totaling USD 1.1172153. The charged/uncertain **upper accounting** is USD 1.1983182, below the authorized USD 5, not a provider invoice. All 30 research responses returned `deepseek-flash`, `finish_reason=stop`; no truncation or semantic retry occurred. Research latency totaled 1,537.389 s, mean 51.246 s, maximum 84.154 s (network/model wall time, excluding image upload). The preflight reserve was USD 3.6073125 with all output token caps, below the hard limit.

The generated public response and call records, seals, exact request manifest and scored outputs are checked into this experiment directory. The private 779 images, 63 request/wire/file-ID artifacts, and preliminary runs remain at the actual server paths listed with size and hash in `ARTIFACT_MANIFEST.json`. Git contains no restricted pixels, raw chain-of-thought, request bodies containing File IDs or credentials. Official File IDs have a 30-day lifetime; reproduction after expiry must re-upload exactly the same hashed media and use a *new explicit authorization* for any further paid inference.
