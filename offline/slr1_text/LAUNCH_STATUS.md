# SLR-1 启动与衔接（当前engineering_r3）

更新时间：2026-09-20T13:40:43+08:00。当前入口engineering_r3/STATUS.json；父目录和engineering_r2是原样保留的工程历史，禁止当作活跃队列重启。

固定18个事件、36个因果数字证据包、72次真实模型请求（原始+重命名重排），比较Z4Q_STABLE、同证据规则与DeepSeek V4.1 Flash。14项语义检查、36/36包检查/规则重排、72/72规则合法性、37/37旧外观描述符精确回归通过；306观测特征、116采样帧。r3复用r2包/提示/基线逐字节相同，REUSE_ACCEPTANCE.json已校验。

官方API deepseek-flash，thinking enabled/high，max_tokens 65536（官方思考默认预算），超时600秒。密钥从Windows系统变量读取，未写入日志。每次调用前检查费用：已用峰值token估算+下一次最坏费用+原工程$0.25储备≤$5，否则自动停止。模型是服务别名，不夸称不可变权重快照。

Windows隐藏后台pipeline PID18128、模型PID73896；核实命令后方可管理，历史PID不作存活凭据。最多4本地逻辑CPU、库1线程，无远端CPU/GPU作业、无训练。前两份若仍被截断自动停；完整响应封存后才独立评价。完成须pipeline_exit_code=0及EVALUATION_ACCEPTANCE，效果尚未评分。

原8192上限4份响应均截断，另1在途请求停止且计费未知；r2的16384上限第一份合法、第二份截断，自动停。两轮失败和所有响应/源码/包均保留，没有用GT判断重试，没有换模型/提示追分。本轮报告必须披露工程调用，不声称全过程只有72次请求。

事件边界/历史目标来自oracle选择，已暴露相关单录像，不是独立盲测或自动事件触发验证。当前主臂只发送匿名数字JSON；裁剪图保留r2/review_crops并已检查2张。未改变SAM/Z4Q输出，不宣称IDF1/HOTA提升。

## 最新实际调用验收 2026-09-20T13:43:59+08:00

engineering_r3已返回4/72，其中3份通过JSON/合法性，1份被拒绝。第二份finish_reason=stop但证据JSON Pointer访问不存在whole字段，按冻结规则保留为无效输出，不改提示/输入重跑；不是再次截断。前两份技术截断门已通过，队列继续。源冻结逐项复核未改。当前所有数值均为运行检查，不是身份效果评分；现有拒绝按最终全合法门会计失败，但固定全队列继续量化其他效果。
