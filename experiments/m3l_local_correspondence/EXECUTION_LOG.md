# M3-L execution log — 2026-09-26

Fixed review base and starting fetched `origin/main`: `8ec0d3b8bd2e7c75dd2d6b3d15b1b0bb6341e16b`; isolated branch `codex/m3l-local-correspondence`. The M3-L user instruction, not old M1/M2-T authority, allowed only this official `deepseek-flash` batch up to 25 inference requests and USD 3. No test GT, training, tracker stage/commit, E2, new IDF1/HOTA or old API rerun. Old M2-T and M2T-F results were read-only. Server preflight checked actual host/user, 54 GiB free on `/home`, RAM and GPU occupancy; only CPU/network was used and no GPU was reserved. Current host and resource values were observations, not standing reservations.

The new server directory `/home/xiongxiong/m3l_local_correspondence_20260926` was created separately. `prepare.py` checked the old B01 source manifest, private frame-local token ledger and 13 real old G PNGs against each image SHA/byte count and the fixed frame list, copied only those 13 images into a private sender media tree, and wrote a real G006/G007 slice audit before generating the remaining pairs. The 12 logical requests contain only two exact G images, all six ROI observations per moment, rounded original relative time, centers and boxes. The model text lacks native/public IDs, A/B/X/Y, GT, old replies, numeric results, original paths and current chat. The sender bubble exposed only this task's sender tree at `/work` plus system Python/TLS/network; a live test found `/home/xiongxiong` inaccessible there. Key bytes were delivered over stdin and not stored in arguments, code, records or Git.

Official [Files API](https://api-docs.deepseek.com/guides/files_api/) and [pricing](https://api-docs.deepseek.com/quick_start/pricing/) were checked 2026-09-26. Peak miss input/output rates USD 0.30/1.20 per million tokens, `max_tokens=65536`; USD 0.11 per attempt ×25 pre-reserved USD 2.75 of the USD 3 hard cap. A synthetic actual-image Files API smoke started **08:03:34.768612 UTC**, recognized red/blue sides and upper-accounted USD 0.0001665. Thirteen research images were then uploaded; all 24 exact file-ID-bearing bodies were frozen at **08:03:54.344431 UTC**, before the first formal start at **08:03:54.357731 UTC**. Every first/repeat pair had identical body SHA-256. Formal calls followed the fixed interleaved schedule without retry, ended at **08:20:50.771160 UTC**, and the 24-response seal was written **08:20:50.782697 UTC**. All 25 inference calls, including smoke, had `finish_reason=stop`; returned model was `deepseek-flash`. Total reported input/output tokens: **30,453 / 238,377**; peak-rate upper accounting **USD 0.2951883** including smoke, not the final invoice. Formal service latency sum **1,014.982 s** (min **10.192 s**, max **82.088 s**).

Only after the full response seal did the scorer validate all hashes/ledger pairs and open the already exposed validation `offline_matches` and original predicted-mask assignments. The first scorer incorrectly collapsed two citation-field type failures into whole-response unparseability (22/24); its full source and outputs are preserved. A separate corrected scorer run reclassified but **did not accept** those coordinate-object citations, produced the authoritative `final_score/` outputs (24/24 decision structures parseable, two attempts with six unusable citations each), and made zero model calls. `SCORER_CORRECTION.md` gives both code and summary hashes. No answer-driven prompt, sample, threshold or baseline change occurred.

P05 and P08 posthoc panels were generated only after scoring, opened against actual pixel bytes and kept private. Their exact paths, sizes, hashes and scope are in `VISUAL_QA.md` and `ARTIFACT_MANIFEST.json`. All public per-attempt and per-token records, the initial score and the corrected score are to be committed; private images, file-ID request bodies, upload ledger, raw API wire, original token ledger and GT matches stay on the server. This log does not claim an independent blind identity review or a full tracking improvement. Delivery-to-main verification is recorded separately after Git push.
