# AO1 frozen input-fidelity comparison

Fixed review base `9e70e02cc7163c75b1e5c34c6d720c501e08aa87`. AO0's sole exposed positive, `AO0-V-GT2-GT6-1322`, retains its contact 1322–1419, query 1498 / global 10798, current streak 1484–1498, complete KEEP/SWAP hypotheses, real committed stage result, and archived scores. Four fixed old E1 model-harm packets are included without rerunning their model calls. No GT raster, unexposed test GT, external model, training, tracker edit, new query, quality threshold or score recomputation is used here.

The paired variable is input evidence, not a new association method. `SOURCE_MANIFEST.json` records real source paths, hashes, time, mask keys, native IDs and 3× mask-to-RGB projection. The authoritative blind package is separately located under `/home/xiongxiong/ao1_input_fidelity_20260924/range_corrected_run/blind/`; it contains only opaque B01–B05 and P1–P3 names, relative time, two complete anonymized mappings, original media and independent localization guides. `SCORE_KEY.json`, this repository, numeric results, and original paths must **not** be given to a reviewer.

- **V0:** Byte-identical old E1 static/temporal sheets. AO0's original low-resolution/red-overlay sheets are retained by hash; only their text header is blanked in the blind copy. The source pixel regions underneath are unchanged. V0 is case-specific, not a common rendering pipeline across positive and negatives.
- **V1:** Exactly V0's source frames and pixel-exact original-resolution uncolored crops covering each V0 fish/interaction tile's actual spatial range. Old E1's context and close crops are both reconstructed (its manifest `crop_xyxy` was only the mask bbox, not either rendered tile); AO0's four contact views are reconstructed without red overlay. Whole 1920×1080 source JPEG bytes are also present as orientation context. A separate white contour crop locates the predicted fish, never replacing the uncolored main crop. No super-resolution or invented detail. Whole-frame fixed ROI is `[0,0,1920,1080]` throughout. A fidelity-only reading must compare V0 with V1's **matched crops**, not use V1's added whole-frame context as if it existed in V0.
- **V2:** V1 plus *every acquired source frame* from `max(split start, registered prediction trigger - 60)` through the unchanged query, copied as original JPEG bytes with monotone relative timestamps; older historical V1 references stay separate. No post-query frame, interpolation, per-frame recentering, colored mid-contact identity trajectory or retrospective key-frame selection. V2 is an explicitly new evidence hypothesis.

For any one case, matched-crop V0→V1 tests representation on identical source frames and spatial fields; the full-frame context in V1 is a separately disclosed broader view, not a pure pixel-resolution contrast. Whole-frame V1→V2 holds the fixed coordinate field and increases only causal frame coverage. Different cases' V0 renderers differ, so across-case V0→V1 improvements cannot be pooled as a single effect. Option order and version aliases are frozen by case index independently of the known correct choice. C1/C2 are complete legal mappings of anonymous packet entities; neither is marked B0/KEEP/SWAP. V1 and V2 main media have no identity color. Endpoint contours are separate, same-color location aids. A judge must choose `C1`, `C2` or `INSUFFICIENT` and cite a tile/relative time/region plus a distinction that rules out the other mapping. One person's later version views are carryover-exposed, not independent first judgments.

`numeric_full.py` applies the unchanged E1C D/A/M definitions to source-visible role samples only. It reconstructs original B0 metadata solely to certify every intermediate native/public/epoch frame, rejects missing or changed lineage, and retains predicted neighbor risk; neighbor presence is not labeled certain mixture. `PARITY_CHECK.json` verifies that this read-only metadata replay equals the archived B0 mapping at every frame through the largest query. V0/V1 have equal numeric source sets and must produce identical edges/decisions. V2 reports additional visible frames, consumed legal samples, null-edge reasons and any changed scores separately. This does not rerun RQ0 as an intervention experiment.

First judgments are not yet collected. The execution context has already seen the exposed AO0 answer and old negative adjudications; its image opening is only QA. An independent blinded reviewer must receive one isolated version of a case at first exposure, without the score key or repository. No conclusion about visual identifiability is inferred from an empty decision ledger, and no human/model choice is converted to IDF1 or HOTA.
