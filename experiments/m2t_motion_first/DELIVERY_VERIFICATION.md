# Actual remote delivery verification

Starting remote `main`: `a27720ff313a27037eb5cbdd9d629506911af7d2`. Experiment commit: `104e3a482d43601338ae589ca9b94df032c66ef5`, created on `codex/m2t-motion-first-main-sync`. Local `main` was fast-forwarded from its older archived tip to the freshly fetched `origin/main`, then fast-forwarded to this experiment commit. No old commit was rewritten, no force push was used, and the worktree was clean.

`git push origin main` returned `a27720f..104e3a4 main -> main`. The first immediate `ls-remote` verification hit an intermittent TLS EOF; a retry using Git's OpenSSL backend and HTTP/1.1, **without disabling certificate verification**, returned `104e3a482d43601338ae589ca9b94df032c66ef5 refs/heads/main`. An independent fetch then made `origin/main` and local `main` equal to that SHA. The fetched commit tree was checked for `FINAL_REVIEW.md`, `RESULTS.md`, `ATTEMPT_RESULTS.jsonl`, `EVENT_RESULTS.jsonl`, `ARTIFACT_MANIFEST.json`, `RESPONSES_SEALED.json`, representative B01/B05 public responses, `research/HANDOFF.md`, and root `AGENTS.md`; all existed. The 25 local downloaded response hashes and sender-code/request-seal hashes also matched the frozen records before commit.

This verification document is an additional public delivery record committed after the first verified push. Its own final remote-tip SHA is given in the task completion response after the second non-force push and fresh remote verification, avoiding a self-referential SHA inside this file. Private pixels, raw wire and provider file IDs remain off Git, with exact paths/sizes/hashes in `ARTIFACT_MANIFEST.json`.
