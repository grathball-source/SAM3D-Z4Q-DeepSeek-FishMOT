# M3-L non-force main delivery

The full M3-L public experiment, including both scorer versions and outputs, all 25 public response records, 24 formal pair rows, 144 object-attempt rows, manifests, code, tests and reports, was committed as `1ba0779bd758708f216bdf0f7ead4adeceef72dc` on `codex/m3l-local-correspondence`. A fresh fetch immediately before integration found `origin/main` still at the fixed review base `8ec0d3b8bd2e7c75dd2d6b3d15b1b0bb6341e16b`; local `main` was fast-forwarded, then pushed normally (no force). A subsequent fetch returned `origin/main=1ba0779bd758708f216bdf0f7ead4adeceef72dc`, and Git tree checks succeeded for this final review, `final_score/PAIR_RESULTS.json`, `responses/P12-REPEAT.json` and the active `research/HANDOFF.md`.

This verification note is an additional public delivery artifact. Its own non-force push and the final fetched remote `main` SHA are recorded in the completion response. No old M2-T/M2T-F file, private pixel, provider file ID, raw API wire, key or GT raster was added to the commit. The only intentionally unsynchronized artifacts are the 68 inventoried restricted server files and four local private visual copies listed in `ARTIFACT_MANIFEST.json`; they are excluded from public Git by design, not omitted reports.
