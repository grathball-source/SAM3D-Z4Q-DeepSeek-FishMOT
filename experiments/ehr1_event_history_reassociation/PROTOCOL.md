# EHR-1 frozen event-history association protocol

Fixed fetched `origin/main`: `0b65e95ce41ef67978fad08fdd098532112b79b4`. Five previously exposed development diagnostics B01–B05, in that order. B01's original predicted contact trigger is the real recovery opportunity; B02–B05 are fixed harmful-change controls. B04/B05 overlap physically. No hidden test GT, SAM rerun, tracker commit, training, DAA restart or E2 occurs.

The original AO1 roles, query frames and complete two-candidate mappings are fixed before this run. For each event, A/B are **hypotheses** frozen before the given prediction trigger; X/Y are bounded current predicted fragments through q. Prediction-native handles never bridge the risky interval in the model packet. Original box centers, time stamps, neighbor flags and source raw HDF5 depth profiles create measurement facts with frame/time, image coordinates, units, quality and epistemic labels. Velocity uses clean, contiguous samples and actual seconds; undefined quantities remain UNKNOWN. The 1-second pre-event window and last 0.5-second current window are fixed rules; stale older AO1 references are exposed as stale. An anonymous intermediate token is never a certified persistent identity. Contact-free return is a prediction-based 3-frame streak, not a GT identity observation.

Five conditions per case: E contains the last pre/current fact and two corresponding mask images, with no velocity/depth/intermediate history; H-2D contains pre, anonymous interaction and current 2D histories; H-D adds actual depth measurements and quality; H-D-REPEAT is byte-identical to H-D; H-D-PERMUTE changes only candidate names/order and is physically inverse-mapped at scoring. H conditions use the same fixed deterministic maximum 12 source mask images: last two pre, eight uniform interaction samples, first current and q; the full intermediate structured observations remain in text, with `image_sent` flags. The image coordinates are fixed full 640×360 mask coordinates and old M2-T's fixed ROI PNGs. Images are prediction masks, with no private RGB or GT raster sent.

The sender's decision task is the same for all conditions: compare each **complete** mapping, cite actual fact IDs for support/conflict, state unresolved assumptions, choose H1/H2/DEFER. A malformed or unsupported explanation cannot silently become a different choice. DEFER remains an abstention, not a correct recovery. Numerical N-H2D/N-HD are frozen before model calls and isolated from the sender. They use the same reliable pre/current histories but cannot exploit anonymous intermediate identity information; they are explicitly endpoint-history references, so they cannot establish VLM necessity against a full same-information algorithm.

Official `deepseek-flash` Files API, thinking enabled/high, JSON response, `max_tokens=65536`, sequential requests, no semantic retry. At most 25 formal requests plus one same-route image smoke and USD 3 peak-rate upper-accounting. All 25 logical requests, full order, actual media hashes and code are frozen before the first formal answer. Every file-ID-bearing body is sealed before the first formal call. Unknown call outcomes count and stop automatic retry. All 25 outputs must be sealed before the independent scorer opens the already exposed key. Restricted pixels, file IDs, raw wire and credentials stay off Git.

The fixed same-host preflight re-read actual raw HDF5 `aligned/raw_depth_mm` for 20 source frames, verified array hashes, original RGB dimensions and exact per-mask whole/core statistics from predicted masks, without GT. Existing depth values are pipeline millimetres; external absolute calibration and a water-surface reference are unverified. Mixed/invalid depth remains qualified or UNKNOWN. No depth number is inferred from the model.

Official documentation checked 2026-09-26: [Files API](https://api-docs.deepseek.com/guides/files_api/) and [model/pricing](https://api-docs.deepseek.com/quick_start/pricing/). The current documented alias is DeepSeek-V4.1-Flash with 1M context; peak cache-miss input/output rates are USD 0.30/1.20 per million tokens. Returned model and actual usage will be recorded; the alias is not an immutable snapshot.
