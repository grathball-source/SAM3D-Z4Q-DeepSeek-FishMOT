# M1 post-score restricted-pixel QA

This is a **post-score self-check**, not an independent blind first judgment or a semantic grounding pass. The seven exact transmitted PNGs listed below were opened at original resolution from the restricted `run_v4/media` directory. They are not included in Git. Their hashes and sizes are in `ARTIFACT_MANIFEST.json`; image IDs and roles are in `REQUEST_MANIFEST.json`. The review covered only B01 and B05, not the other three cases or the 779-file media collection.

| Case | Images actually opened | Observation limited to visible pixels | Consequence |
| --- | --- | --- | --- |
| B01 first P2 | `I002` (`f4491cc6…`), `I004` (`8b2a69ce…`), `I007` (`974d7bfe…`), plus `I010` (`1fe0183b…`) named in the model's explanation but not as its own evidence object | Each crop shows several dark, overlapping fish. The claimed head-on/side-on and caudal features cannot be unambiguously assigned to the intended role from these uncolored crops alone. | The first P2 choice was correct against the exposed key, but its three citations do **not** establish independently verified visual identity evidence. The exact-input repeat chose the opposite candidate. |
| B05 first P2 | `I002` (`558f0dd0…`), `I004` (`3c576d6f…`), `I006` (`5feb43f4…`) | A red eye is visible in the B and X crops. A's asserted row of pale flank spots is not clearly resolved, and the X crop truncates most of the body, so the claimed full-body/no-spots comparison cannot be certified. | Some local appearance words are pixel-plausible, but the complete identity inference remains unverified; repeat and permutation chose the harmful opposite candidate. |

Across **all 15 image-bearing P2 first/repeat/permuted responses**, the model cited three images per response but **zero** newly added `whole_frame` images. Every formal citation pointed to a matched crop already present in P1. This does not prove the full-frame context had no latent influence, but it prevents attributing the P1→P2 choice changes to a located whole-frame observation. Citation format was valid in 29/30 research responses; the sole exception is `D03S` (`REGION_NOT_OBJECT`). That format failure did not replace its bound candidate choice. The truth status is in `EVENT_RESULTS.jsonl`, not inferred from the cited text.
